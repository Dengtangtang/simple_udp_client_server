This is a very simple udp client and (echo) server.

Client sends a message to server and the echo server sends back what it got from client and add sequence number at the head of the message; client then output the received message to terminal.

It is a warm up for my next practice for ftp server.